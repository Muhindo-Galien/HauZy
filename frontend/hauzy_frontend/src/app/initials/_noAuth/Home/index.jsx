import React from 'react';
import HomePage from '../../../modules/_noAuth/Home';

const Home = () => {
    return (
        <div>
            <HomePage/>
        </div>
    );
};

export default Home;
